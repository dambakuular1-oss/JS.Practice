function квадрат(a) {
  const площадь = a * a;
  const периметр = 4 * a;
  const диагональ = +(a * Math.sqrt(2)).toFixed(2);

  return { площадь, периметр, диагональ };
}

// Пример использования
const сторона = 5;
const результат = квадрат(сторона);

console.log(`Сторона: ${сторона} см`);
console.log(`Площадь: ${результат.площадь} см²`);
console.log(`Периметр: ${результат.периметр} см`);
console.log(`Диагональ: ${результат.диагональ} см`);